<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Excluded Session Keys
    |--------------------------------------------------------------------------
    |
    | 這裡設定哪些 session key 不要出現在 SessionReader::all()。
    | 你可以加上 Laravel 內建的 (_token, _previous, _flash, errors)
    | 或任何專案自訂要排除的 key。
    |
    */

    'excluded' => [
        '_token',
        '_previous',
        '_flash',
        'errors',
    ],

    /*
    |--------------------------------------------------------------------------
    | Prefix-based exclusions
    |--------------------------------------------------------------------------
    |
    | 這裡設定要排除的前綴。例如 Laravel 登入時會產生 login_web_xxx，
    | 只要符合這些前綴就會被排除。
    |
    */

    'excluded_prefixes' => [
        'login_',
    ],

];
